# BETA-10 Next Roadmap

Next stage: **GENERAL AVAILABILITY READINESS** — preparation only, not activated.

The next roadmap must close remaining retry and dead-letter conditions, promote a verified stable channel candidate, preserve rollback evidence, and re-run production security, tenant isolation, reconciliation, observability, support, backup/restore and release gates before any general rollout.

Preserve schemaVersion = 4 and syncContract = schema_version_4 until an explicit versioned migration changes those contracts.
