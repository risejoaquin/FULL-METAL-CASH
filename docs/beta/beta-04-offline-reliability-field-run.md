# BETA-04 — Offline Reliability Field Run

Field run controlado offline -> reconnect -> push/process -> duplicate push -> pull/read models -> reconciliación SQL.

Contrato obligatorio: `schemaVersion = 4`, `syncContract = schema_version_4`.
