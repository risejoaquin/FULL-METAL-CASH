# Hotfix Public GA Burn-In 01 — Validation Commands

Use the same Public GA Stability Burn-In command as before. Expected validator version:

`PUBLIC-GA-STABILITY-BURN-IN.1.1-server-wait-pressure-semantics`

The sample PASS line now reports `serverWait`, `legacyWaitEvent`, `clientWait`, and `totalConnections`.
